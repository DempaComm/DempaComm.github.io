# Typst変換比較試験

TeX原稿を変更せず、TylaxとPandocでTypstへ変換し、TypstコンパイラでPDFを生成して比較する。
これは公開処理ではなく、結果はGit管理外の `_experiments/` にだけ置く。

## 使用ツール

- Typst 0.15.1以降: Typstソースのコンパイル
- Tylax 0.3.7以降: 原稿全体を対象にする第一候補
- Pandoc 3以降: 比較基準

macOSでは次の順に導入する。

```sh
brew install typst pandoc rust
cargo install tylax
```

導入後にバージョンを確認する。

```sh
typst --version
t2l --version
pandoc --version
```

## 代表4分類を変換する

リポジトリのルートで実行する。

```sh
python3 scripts/paper_tool.py typst-trial
```

対象は `experiments/typst-trial.json` に記録している。

- 単純なTeX
- 図や参照を含むTeX
- 複雑なTeX
- BibTeXを使う原稿

原稿ごとに次のファイルを `_experiments/typst/記事番号/` へ生成する。

- `tylax.typ`、`tylax.pdf`、変換・コンパイルログ
- `pandoc.typ`、`pandoc.pdf`、変換・コンパイルログ

全体結果は `_experiments/typst/report.json` に記録する。出力先が空でなければ上書きせず停止する。
再試験では別の出力先を指定する。

```sh
python3 scripts/paper_tool.py typst-trial \
  --output _experiments/typst-2
```

特定原稿だけを試すこともできる。

```sh
python3 scripts/paper_tool.py typst-trial 2015-08-28-01 \
  --output _experiments/typst-primes
```

## 安全条件

変換時は原稿フォルダを一時領域へコピーし、Tylax、Pandoc、Typstはコピーだけを読み書きする。
元TeX、PDF、BibTeX、BST、図版、`paper.json` は変更しない。試験出力は自動公開されず、
`report.json` の `publishable` は常に `false`、`manual_review_required` は常に `true` である。

## 安全な補正パッケージ

Tylaxの生出力を保存したまま、意味が一意な補正だけを別ファイルへ適用する試作は
[`packages/dempa-typst-converter/`](../packages/dempa-typst-converter/README.md) に置く。
このフォルダは独自のPythonパッケージ定義、Apache-2.0ライセンス、Typstスタイル、人工的な
最小例、テスト、導入・変換・公開手順を持ち、将来はそのまま独立リポジトリへ切り出せる。

現在の補正器は `\neq` に加え、対応する開始・終了マーカーを持つ定理構造、先頭ラベル、
変換済みラベルへの番号参照、明示的な終端記号を持つ証明を変換する。対応しない境界、重複
ラベル、未解決参照、未知のLaTeX命令が残る場合は `main.typ` を生成せず、JSON報告を残して
停止する。個別記事の手作業で得た補正を、確認なしに一般規則へ昇格させてはならない。

パッケージ単体の詳しい変換手順は
[`docs/CONVERSION_WORKFLOW.md`](../packages/dempa-typst-converter/docs/CONVERSION_WORKFLOW.md)、
必要アプリの導入は
[`docs/INSTALLATION.md`](../packages/dempa-typst-converter/docs/INSTALLATION.md) を参照する。

## 人が比較する項目

1. 日本語が欠落または文字化けしていないか。
2. 数式、定理環境、番号、相互参照が元PDFと一致するか。
3. 図版、TikZ、引用、参考文献が欠落していないか。
4. 変換された `.typ` が人間に編集できる構造か。
5. 元PDFと意味およびページ内容が変わっていないか。

変換・コンパイルに成功しても公開承認にはならない。公開機能は、4分類の差異を確認して
派生ファイルの保護・履歴・再生成規則を別途設計した後に検討する。

## 2026-07-28の初回結果

Typst 0.15.1、Tylax 0.3.7、Pandoc 3.10.1で代表4分類を変換した。Pandocは4件すべて、
Tylaxは3件で日本語を含む `.typ` を生成した。「図や参照を含むTeX」のTylax変換は
180秒で停止した。生成された7件の `.typ` は、いずれも未補正ではPDFコンパイルに至らなかった。

- 単純なTeX:
  - Tylaxは短く比較的読みやすい出力を生成したが、`\text` 内に残ったLaTeX数式を
    Typstの変数として解釈して停止した。
  - Pandocは文書構造を保持したが、命題への参照先を参照可能な要素として生成できず停止した。
- 図や参照を含むTeX:
  - Tylaxは変換が制限時間を超えた。
  - Pandocは本文を生成したが、引用・定理参照のラベルが解決されず停止した。
- 複雑なTeX:
  - TylaxはLaTeXの寸法命令、独自見出し、区切り記号などをTypstへ未変換のまま残した。
  - Pandocは本文を広く保持したが、数式中で連結した文字列を未定義変数として解釈した。
- BibTeXを使う原稿:
  - Tylaxは列挙ラベルと独自構造のLaTeX命令を残した。
  - Pandocは引用キーと直後の日本語を一つの参照名として解釈し、参照を解決できなかった。

初回結果から、どちらも現状のまま公開用一括変換器にはできない。ただし、変換器ごとの生出力と
ログを同じ形式で比較する基盤は完成した。次は単純なTeXを対象に、元TeXを変更せず、
変換後だけに適用する明示的な補正规則の範囲を決める。参照や数式の意味を推測する補正は
自動化せず、失敗として残す。

## 2026-08-01の補正試作

単純なTeX「素数の無限性」のTylax出力について、残存した `\neq`、定理構造、命題参照、
証明見出しを実験領域内だけで補正し、Typst PDFが1ページで生成できることを確認した。
この個別補正から、数式トークンのように意味が一意な処理と、定理・参照のように構造解析が
必要な処理を分離した。前者の最小規則と共通スタイルの公開用雛形を
`packages/dempa-typst-converter/` に追加した。この時点では後者を未実装として安全に停止させた。

## 2026-08-01の自動構造補正

前項で手作業していた定義・命題・定理・証明、共有番号、ラベル参照を補正器へ実装した。
番号は記事固有の `2` や `3` へ置換せず、Typstの番号付き要素とラベル参照として保持する。
同じTylax生出力を新しい補正器へ通し、`main.typ`、`dempa-style.typ`、JSON報告を生成して、
Typst 0.15.1でA4・1ページのPDFになることを確認した。定義1、命題2・3、定理4と、証明中の
命題2・3への参照が紙面に表示され、文字の重なりや欠落がないことを目視した。

## 2026-08-21の第二練習記事

「代数学の基本定理2」 (`2015-09-15-02`) を、素数の記事より一段複雑な練習記事に選んだ。
原稿は図版と外部BibTeXを持たず、事実3件、補題1件、定理1件、証明4件、手書き参考文献を
含む。Tylaxは短時間で生出力を生成したが、`Fact` と `Lemma` を壊れた強調構文へ変換したため、
従来の補正器は安全と誤判定した後にTypstコンパイルで失敗した。

固定表示の事実・補題、事実・例環境、`\displaystyle` 由来の `display`、参考文献の図扱いを
補正し、CLI自身が一時領域でTypstを実コンパイルするようにした。任意題名と本文の境界は
Tylax出力だけから推測せず、`--latex-source` で元TeXを読み取り専用の手掛かりとして渡した
場合に限り、環境種別・順序・題名先頭が完全一致したものを分離する。不一致なら停止する。
補正後はTypst 0.15.1でA4・2ページのPDFとなり、事実1〜3、補題4、定理5、証明終端、数式
番号1〜6、参考文献1件が表示された。全ページで文字の重なり、切れ、黒塗りがないことを
確認した。元PDFはローカルPopplerの旧日本語フォント対応不足で日本語を描画できなかったため、
TeX本文および元PDFの数式・番号・ページ構成を併用して比較した。

## 2026-08-21の第三練習記事

「順序数の位相的重み」 (`2015-12-21-04`) を隔離変換した。数字だけのラベル4件と参照4件、
明示的な単一行境界を持つ古い証明2件、`\card` の固定表示9件を人工的な最小テストから補正した。
Tylaxが `\end{document}` 後の `comment` 環境を出力へ混入させたため、対応する開始・終了
マーカーが揃う場合だけ内容を除外し、片側だけなら安全停止するようにした。

元TeXの表示数式はすべて番号なしだったが、Tylaxは一律番号を付けていた。元TeXを読み取り専用の
手掛かりとして、番号なし表示数式だけの場合に限り全体番号を除去する。番号方式が混在する場合は
安全停止する。補正後はTypst 0.15.1でA4・1ページとなり、命題1〜4、定理5、参照、証明終端、
数式を全ページ目視確認した。元TeXの `\date{\today}` による日付は変換実行日になる。
利用者確認により、Typst派生版ではこの変換日をそのまま表示する。

## 2026-09-08の第四練習記事

「バナッハの不動点定理」 (`2015-10-27-01`) を隔離変換し、定義1件、命題1件、定理2件、
証明3件、手書き参考文献1件を補正した。証明のうち2件は改行命令または空行を含む複数行の
Tylax出力だったが、既存の明示的な四角終端記号による境界解析で3件とも補正できた。

補正報告には阻害事項・要確認事項がなく、Typst 0.15.1でA4・3ページのPDFを生成した。
全ページで証明終端、定理構造、数式、参考文献、文字の重なり・切れがないことを目視確認した。
元PDFはローカルPopplerの旧日本語フォント対応不足で日本語を描画できなかったため、TeX本文と
元PDFの数式・ページ構成を併用して比較した。既存の隔離Tylax出力から四角終端記号のない実例は
見つからなかったため、その場合の一般則は推測で追加していない。

## 2026-09-08の第五練習記事

「The Horrible lemma」 (`2015-12-21-03`) で、番号付き `align` 1件（2行）と番号なし表示数式
7件が混在する場合を検証した。TylaxとPandocはどちらもTypst PDFを生成できたが、元PDFの
番号を保持できなかった。Tylaxは番号なし7件を含む全8数式ブロックへ (1)〜(8) を付け、
`align` の2行を1個の番号へ平坦化した。Pandocは `align` の式番号 (1)、(2) を両方とも失った。

補正器は元TeXから番号方式の混在を検出し、終了コード2で安全停止した。JSON報告だけを残し、
補正版 `main.typ` は生成していない。元PDF、Tylax版、Pandoc版の全ページを目視し、現行の
安全停止が必要であることを確認した。Tylax出力では複数行数式の行ごとの番号境界も失われるため、
単純な番号除去では対応できない。復元を実装するなら、元TeXと変換出力の数式単位・行単位の
厳密な対応付けと、不一致時の安全停止を先に設計する必要がある。

## 2026-09-08の第六練習記事

「有理数の順序集合としての特徴づけ」 (`2017-05-07-01`) で、番号付き `align` 1件（2行）、
番号なし表示5件、定理・補題のラベルと参照を含む第二の混在例を検証した。Tylax版とPandoc版は
別々の構文・参照問題でTypstコンパイルに失敗した。

この原稿で判明した証明境界の問題は、実原稿本文をコピーしない人工例を先に追加して修正した。
文字列、コメント、URLを字句的に保護し、その内部の偽終端を使わず、明示的な四角終端記号を持つ
証明2件をともに補正できる。反復する同一の `_Proof._` が一部だけ残る場合も、未変換の開始だけを
生入力上の正確な位置へ対応付ける。保護領域内の `comment` 環境マーカーも削除しない。

再検証では証明補正2件、証明境界診断0件となった。さらに数式内の `\rm dom` と `\rm cod`
15件を、既知の `op("dom")` と `op("cod")` に限定して補正した。未知のローマン体名称や数式外の
同形文字列は変換せず停止する。元TeXを読み取り専用ヒントとして渡す通常経路では、残る停止理由は
番号方式の混在だけになった。

続いて、元TeXで `\item[項目名]\mbox{}\\` の明示境界を持つ `description` 3項目について、
項目数と数式外の項目名断片がTylax出力と一致する場合だけTypstの `/ 項目: 本文` へ復元した。
ヒントなし・件数不一致・項目名不一致では停止する。番号情報だけを外した構文検査はこの箇所を
通過し、次の未対応トークン `sect` で停止した。元TeXの対応箇所は共通部分記号 `\cap` である。
