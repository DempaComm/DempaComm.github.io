# -*- mode: Perl -*-
# The historical sources load a package named fbox, but use only the standard
# LaTeX box commands that LaTeXML already provides. No additional definitions
# are needed for the HTML derivative.
package LaTeXML::Package::Pool;
use strict;
use warnings;
use LaTeXML::Package;

1;
